

```bash
sudo dnf install \
        screen gawk grep sed openssl openssh sshpass git rsync findutils curl wget criu \
        rust bc zsh bash lua ruby go python3 fish nim docker \
        mariadb
```
